{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "served-origin",
  "description": "Native shim permissions for the SPA served by the embedded server (or Vite in dev). Loopback origins only.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  },
  "permissions": ["notification:default"]
}
