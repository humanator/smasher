// ABOUTME: Tauri build script: generates the app context from tauri.conf.json and capabilities.
// ABOUTME: Required by tauri::generate_context!() in main.rs.

fn main() {
    tauri_build::build()
}
